export default function App() {
  return (
    <main className="app-foundation" aria-label="AI TriView">
      <section className="foundation-panel">
        <p className="foundation-kicker">AI TriView</p>
        <h1>Base dashboard foundation</h1>
        <p>
          Global theme tokens, spacing, typography, and accent colors are ready
          for the dashboard issues that follow.
        </p>
      </section>
    </main>
  );
}
